/* assets/js/app.js (commented, robust API helper) */(function highlightNav(){const page=(location.pathname.split('/').pop()||'index.html').replace('.html','');document.querySelectorAll('header.nav a').forEach(a=>{const match=a.getAttribute('data-page');if((page==='index'&&match==='home')||match===page)a.classList.add('active');});})();(function setYear(){const s=document.getElementById('year');if(s)s.textContent=new Date().getFullYear();})();window.toast=function(message){let t=document.querySelector('.toast');if(!t){t=document.createElement('div');t.className='toast';document.body.appendChild(t);}t.textContent=message;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),3000);};window.api=function(path,options={}){const base=String(window.API_BASE||'').trim();if(!base){throw new Error('API base URL not configured.');}return fetch(`${base}${path}`,{headers:{'Content-Type':'application/json',...(options.headers||{})},...options}).then(async r=>{if(!r.ok){const data=await r.json().catch(()=>null);const msg=data&&(data.error||data.message);throw new Error(msg||`HTTP ${r.status}`);}if(r.status===204)return null;return r.json();});};